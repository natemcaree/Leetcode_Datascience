class Solution:
    def plusOne(self, digits: list[int]) -> list[int]:
        new_digits = str(digits)
        print (new_digits)
        
        
    digits = [9,9]
    print(plusOne(plusOne, digits))
    
    # Initial idea was to use if statements and manually append 1.
    # New idea: condense into an int, perform math, split into list again. 